import { PageSkeleton } from "@/components/ui/ContentSkeleton";

// Next.js route-level loading UI — shown while an (app) route segment loads.
export default function Loading() {
  return <PageSkeleton />;
}
