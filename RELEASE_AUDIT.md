# Monofarm — Аудит готовності до релізу

**Дата:** 2026-05-29 · **Гілка:** main · **Коміт:** b3f239b

> Цей файл замінює застарілий `ROADMAP.md` (там описані баги, які вже виправлені — див. розділ «Що вже зроблено»).

---

## Підсумок

| Вимір | Готовність |
|---|---|
| **Продукт (двигун застосунку)** | **~85%** |
| **Функціональна цілісність (ферма↔склад як 1 конвеєр)** | **~40%** |
| **UX-полірування** | **~55%** |
| **Go-to-market (запуск + реклама)** | **~30%** |
| **ЗАГАЛОМ до релізу з рекламою** | **~60%** |

**Головна теза:** функціонал майже весь є і він глибокий. Але проєкт «не відчувається цілісним» з **чотирьох** причин, у порядку важливості:
0. 🔴🔴 **Архітектурна:** ферма і склад — два острови, не один конвеєр «друк→товар→продаж→гроші». **Це пріоритет №1.**
1. 🔴 Вітрина: нема лендінгу, юр-сторінок.
2. 🟠 Полірування UX: нативні діалоги, нема обробки помилок, нема мобілки.
3. 🟡 Інструменти запуску: трекінг, email, моніторинг.

---

## 0. Функціональна цілісність — ферма і склад як один конвеєр 🔴🔴

**Пріоритет №1.** Кожен модуль *всередині* майже завершений (~90%), але дві половини продукту — **принт-ферма** і **склад/ERP** — працюють як два паралельні острови, не зшиті в один виробничий конвеєр.

### Діагноз (підтверджено в коді)

- **`PrintTask` не має `product_id`** (`models/task.py`) — те, що друкуєш, це вільний текст `title`, не прив'язаний до SKU.
- **Завершення завдання друку** (`api/tasks.py:300+`) списує філамент і рахує `material_cost_uah` — але **НЕ створює готову продукцію на складі** (немає `PRODUCTION_IN` для Product).
- **`ProductionBatch` не має зв'язку з принтером / gcode / завданням друку** (`models/warehouse.py:272`) — окремий ручний складський об'єкт.
- Два різні поняття «виробництва»: `PrintTask.pieces_ok` (ферма) і `ProductionBatch.good_qty` (склад) — ніколи не зустрічаються.

**Наслідок:** надрукував 50 шт → щоб вони стали товаром, треба **окремо вручну** створити й закрити партію. Ланцюг «замовлення→друк→товар→продаж→гроші» розірваний посередині. Саме це створює відчуття «двох застосунків під одним логіном».

### Конвеєр, який має бути

```text
Замовлення (попит)
   ↓ створює
Партія виробництва ←──лінк──→ Завдання друку на фермі
   ↓ друкуєш на принтерах        ↓ pieces_ok рахуються автоматично
   ↓ закриття = PRODUCTION_IN (готова продукція на склад)
   ↓ реальна собівартість друку → AVCO товару
Залишок готової продукції → резерв → відвантаження (SALE_OUT) → дохід/маржа
```

### Що зробити (детальний playbook — у ROADMAP.md, Етап 1)

| # | Зміна | Де |
|---|---|---|
| 0.1 | `PrintTask.product_id` (nullable FK → `wh_products`) | `models/task.py` + міграція |
| 0.2 | Лінк `ProductionBatch` ↔ завдання друку / принтери | `models/warehouse.py` |
| 0.3 | Авто-`PRODUCTION_IN` при `done` + `pieces_ok` | `api/tasks.py` |
| 0.4 | Реальна собівартість друку → AVCO товару | `api/tasks.py` + `_update_avco` |
| 0.5 | «У виробництво» з sales-order на бракуючу к-сть | `api/warehouse.py` |

**⚠️ Ризик ВИСОКИЙ** — чіпає stock ledger + AVCO. `_update_avco` ПЕРЕД `_apply_movement`. Тести обов'язкові. **Пріоритет 0 дає ~80% відчуття цілісності.**

### Дірки на стороні ERP (нижче за цілісність, вище за полірування)

- **Немає закупівель (Purchase Orders)** — закупівля лише ручним рухом `PURCHASE_IN`. Дзеркало sales-Order відсутнє.
- **Резерв ніколи не звільняється** — `reserved_qty` блокується безстроково.
- **Мультивалютність косметична** — `Order.currency` є, конвертації немає.

---

## 1. Готовність по функціях (продукт)

### Принт-ферма

| Функція | % | Стан |
|---|---|---|
| Dashboard (живий грід принтерів) | 95% | Працює, recharts, статуси |
| Принтери (список, деталі, керування) | 90% | Moonraker + Bambu + pause/resume/cancel/webcam |
| Деталь принтера (`[id]`, 1655 рядків) | 90% | Температури, ETA, прогрес, контроли |
| Завдання друку (kanban) | 90% | todo/in_progress/done |
| План (drag-and-drop) | 85% | Працює, є double-submit guard |
| Черга | 85% | Працює |
| Історія | 85% | Працює, але **немає empty-state** |
| Аналітика друку | 90% | Реальна (API + recharts): summary/daily/printers/filament |
| Файли (gcode/3mf, 956 рядків) | 90% | Upload, SendModal, slot remapping, OctoPrint shim |
| Філамент / матеріали | 90% | Інвентар, low-stock, друк етикеток |
| Локальний агент (тунель) | 85% | WebSocket, pairing, версіонування |

### Склад / ERP (ваш диференціатор — найзріліша частина)

| Функція | % | Стан |
|---|---|---|
| Номенклатура (1931 рядок) | 95% | CRUD, фото, CSV-імпорт з маппінгом, фільтри |
| Специфікації (BOM + операції) | 90% | Матеріали, операції, розрахунок собівартості |
| Залишки (по складах + комірках) | 90% | Inline-редагування порогів, поповнення, replenish |
| Рухи (ledger) | 90% | Cursor-пагінація ✓, фільтри, пошук |
| Замовлення (1062 рядки) | 90% | Резерв→відвантаження, **часткова оплата ✓** (`/payments`) |
| Виробництво (партії) | 85% | Open→closed, **live printed_qty ✓**, AVCO |
| Фінанси (cash flow) | 85% | Доходи/витрати, summary. **Немає empty-state** |
| Контрагенти | 90% | Постачальники/клієнти, баланс |
| Склади + зони + стелажі + комірки | 90% | Bin-level WMS, putaway/relocate |
| Аналітика складу | 90% | **Баг доходу ВИПРАВЛЕНО** (`total_revenue`, не `cost_price`) |
| Категорії | 90% | CRUD, кольори |

### Платформа

| Функція | % | Стан |
|---|---|---|
| Білінг (Lemon Squeezy) | 85% | checkout/webhook/cancel/status готові. Треба юр-сторінки + бойове тестування |
| Налаштування (1736 рядків) | 90% | Підписка, агент, орг, Bambu, Telegram, **тарифи (електрика/праця) редаговані ✓**, профіль, мова/валюта |
| Auth / мультитенантність | 75% | JWT, org-ізоляція, ролі. **Нема reset/verify/rate-limit** |
| i18n (uk/en) | 85% | Працює |
| Telegram-бот | 90% | Сповіщення, magic-link, per-org токени |

---

## 2. UX-полірування — чому «не відчувається цілісним» 🔴

Це найбільший важіль. Фічі є, але дрібниці видають «недороблене».

### 2.1 Нативні `alert()` / `confirm()` — 35 викликів у 16 файлах

**Проблема:** браузерні діалоги виглядають аматорськи й ламають візуальну мову.
**Файли:** materials, settings, tasks, cashflow, categories, orders, production, products, specs, stock, warehouses, warehouses/[id], PrinterCard, PrinterDetailModal, PrinterGroupsModal, UsersSection.
**Рішення:** єдина toast-система (напр. `sonner`) + компонент `ConfirmDialog` на базі наявного `Modal`. Замінити всі 35.
**Складність:** середня · **Вплив:** дуже високий.

### 2.2 Немає обробки помилок на рівні роутів

**Проблема:** нема жодного `error.tsx` / `global-error.tsx`. Будь-який краш рендера → сирий екран помилки Next.js.
**Рішення:** `app/error.tsx` (fallback з кнопкою «Оновити») + `app/global-error.tsx`.
**Складність:** мала · **Вплив:** високий.

### 2.3 Немає сторінки 404

**Проблема:** нема `not-found.tsx`. Невірний URL → дефолтний 404 Next.js.
**Рішення:** `app/not-found.tsx` у стилі застосунку.
**Складність:** мала · **Вплив:** середній.

### 2.4 Немає мобільної верстки 🔴

**Проблема:** `(app)/layout.tsx` має фіксований `pl-14`/`pl-[220px]`, сайдбар завжди видимий, **жодного брейкпоінта**. Таблиці складу не адаптовані. На телефоні застосунок майже непридатний.
**Чому критично для реклами:** більшість кліків з реклами — мобільні. Людина клікає оголошення з телефона → бачить зламаний інтерфейс → йде.
**Рішення:** мобільний drawer-сайдбар (hamburger), `md:` брейкпоінти, горизонтальний скрол/картки для таблиць.
**Складність:** велика · **Вплив:** дуже високий.

### 2.5 Відсутні empty-states

**Проблема:** dashboard, history, cashflow, production, analytics не мають станів «ще немає даних». Новий юзер з порожнім акаунтом бачить голі екрани.
**Рішення:** дружні empty-states з CTA («Додай перший принтер», «Створи перше замовлення»).
**Складність:** мала · **Вплив:** високий (це перше, що бачить новий клієнт з реклами).

### 2.6 Немає route-level loading

**Проблема:** нема `loading.tsx`. Перехід між сторінками — без skeleton.
**Рішення:** skeleton-loading на важких сторінках.
**Складність:** мала · **Вплив:** низький-середній.

---

## 3. Go-to-market — блокери запуску з рекламою 🔴

### 3.1 Немає лендінгу

Корінь `/` просто редіректить на `/login` або `/dashboard`. **Рекламу нема куди вести.** Потрібна маркетингова сторінка: value prop, фічі, ціни, скріншоти, CTA «Спробувати безкоштовно». **Блокер №1.**

### 3.2 Немає юр-сторінок (Умови + Політика конфіденційності)

**Жорсткий блокер:** Lemon Squeezy вимагає; Google Ads і Meta не пускають оголошення без Privacy Policy URL; GDPR. Потрібні `/terms` і `/privacy`.

### 3.3 Немає трекінгу конверсій (GA4 / Meta Pixel)

Без цього **неможливо виміряти ROI реклами**. Мінімум: GA4 + Meta Pixel + подія на реєстрацію та на upgrade плану.

### 3.4 Немає email-сервісу

Нема скидання паролю → юзер, що забув пароль, залочений назавжди. Нема верифікації email. Для платного SaaS — обов'язково. Рішення: Resend/SendGrid + ендпоінти `forgot-password`/`reset-password`.

### 3.5 Немає моніторингу помилок (Sentry)

Під час сплеску трафіку з реклами ви сліпі до крашів на проді. Підключити Sentry на бек і фронт.

### 3.6 Слабке SEO / соц-шеринг

`title` просто «monofarm», нема OG-картинки. Шер у соцмережах виглядатиме порожньо.

### 3.7 Dev-сторінка в проді

`/design-system` (33KB) досі в production-білді. Прибрати або закрити за роллю.

---

## 4. Менші продуктові прогалини 🟡

- **Onboarding лише для агента** (install/waiting/done). Немає продуктового first-run туру: «створи перший товар / додай принтер». Новий клієнт з реклами не розуміє, з чого почати.
- **Валюта тільки в localStorage** — не синхронізується з орг на бекенді (модель має `currency`). Між пристроями розходиться.
- **0 фронтенд-тестів** (бек має 68). Ризик регресій при швидких змінах перед запуском.
- **Унікальність SKU/штрихкоду** по орг — варто перевірити, чи захищено від дублів.
- **Термін резервування замовлення** — резерв тримає склад без авто-звільнення.
- **Рахунок/документ для замовлення** (PDF) — корисно для B2B-клієнтів.

---

## 5. Що вже зроблено (ROADMAP.md застарів)

Ці пункти зі старого роадмапу **вже виправлені** — не витрачати на них час:

- ✅ Баг доходу в аналітиці (тепер `total_revenue`, маржа рахується правильно)
- ✅ Assembly-сторінка-мок — видалена
- ✅ `/filament` — це чистий редірект на `/materials` (не проблема)
- ✅ Пагінація рухів — cursor-based
- ✅ Часткова оплата замовлень — ендпоінт `/orders/{id}/payments`, статуси unpaid/partial/paid
- ✅ Live `printed_qty` партії — ендпоінт є
- ✅ Тарифи електрики/праці — в моделі Organization + редаговані в Settings
- ✅ Sidebar — чистий, без посилань на видалені сторінки

---

## 6. Пріоритетний план до релізу

Порядок = за важливістю. Детальний playbook кожної задачі — у **ROADMAP.md**.

### Етап 1 — Цілісність: ферма = склад 🔴🔴 (пріоритет №1)

1. `PrintTask.product_id` + UI вибору SKU
2. Лінк `ProductionBatch` ↔ завдання друку / принтери
3. Авто-`PRODUCTION_IN` при завершенні друку
4. Реальна собівартість друку → AVCO товару
5. «У виробництво» з sales-order

> Без цього продукт = два застосунки. Це дає ~80% відчуття цілісності.

### Етап 2 — Блокери реклами 🔴 (без цього не вмикати трафік)

1. **Лендінг** на `/` (value prop + ціни + CTA) — ⛔ контент від користувача
2. **`/terms` + `/privacy`** — ⛔ реквізити від користувача
3. **GA4 + Meta Pixel** + подія реєстрації — ⛔ ID-ключі
4. **Email: скидання паролю** (Resend/SendGrid) — ⛔ API-ключ

### Етап 3 — Полірування 🟠 (щоб виглядало завершеним)

1. **Toast + ConfirmDialog**, замінити 35 нативних діалогів
2. **Мобільна верстка** (drawer-сайдбар + адаптивні таблиці)
3. **`error.tsx` + `not-found.tsx`**
4. **Empty-states** на всіх головних сторінках + CTA
5. Прибрати `/design-system` з проду
6. **Продуктовий onboarding** (first-run тур)

### Етап 4 — Стійкість + дірки ERP 🟡

1. **Sentry** (бек + фронт) — ⛔ DSN
2. **Rate-limit** на `/auth/login` і `/auth/register`
3. **Закупівлі (Purchase Orders)** + приймання по PO
4. **Термін резерву** (`reserved_until` + авто-звільнення)
5. **Мультивалютність** (exchange_rate) — ⛔ рішення продукту
6. Email-верифікація · OG-картинка · синк валюти · перші фронт-тести

---

## 7. Орієнтир по зусиллях (реалістично, зі мною)

**Увесь код по всіх етапах — ~1-2 сфокусовані дні**, а не тижні. Розтягують не швидкість коду, а зовнішні залежності (⛔ акаунти/ключі/контент/тестування на реальних принтерах).

| Етап | Чистий код | Вузьке місце |
|---|---|---|
| 1. Цілісність | 1-3 год | Тести обліку/AVCO, щоб не зламати гроші |
| 2. Реклама | 1-2 год | ⛔ акаунти + лендінг-контент + юр-реквізити |
| 3. Полірування | 2-4 год | Візуальні ітерації мобілки |
| 4. Стійкість+ERP | 2-4 год | Закупівлі — повноцінна фіча |

Після Етапів 1-3 → **~90% готовності до публічного релізу з рекламою**.
