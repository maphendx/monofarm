# Bruno collection — monofarm API

1. Open Bruno → **Open Collection** → ця папка.
2. Environment **local** → вкажи `adminEmail` / `adminPassword`.
3. **01-auth → Login** → далі всі запити з Bearer token.

Повний довідник: [docs/API_BRUNO.md](../../docs/API_BRUNO.md)

94 HTTP-запити + WebSocket `/api/agent/connect` (тестуй окремо).
