meta {
  name: Create API key
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/api-keys
  body: json
}

body:json {
  {
    "name": "OrcaSlicer",
    "scopes": "files:upload"
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("apiKey", res.body.key);
  }
}
